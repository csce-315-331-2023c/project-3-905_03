import React from 'react';

interface MenuItem {
  name: string;
  link: string;
}

interface MenuProps {
  items: MenuItem[];
}

const Menu: React.FC = () => {
  return (
    <nav>
     
    </nav>
  );
};

export default Menu;
